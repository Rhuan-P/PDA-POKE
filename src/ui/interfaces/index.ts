/**
 * Barrel export para interfaces da UI Layer
 * Centraliza todas as interfaces em um único ponto de importação
 */

// Interfaces da camada de UI serão adicionadas aqui
// Exemplo: export { IBattleComponent } from './IBattleComponent';
